python如果是在一个文件中定义和调用函数
会发现 调用一定要在定义前面 有先后顺序
但是如果定义和调用分开两个文件就不用考虑先后顺序  定义函数集中一个文件  调用函数集中一个文件

 因为导入永远在最前面执行
 
 header最好也放在一个文件中 避免重复导入 一层层引用 
 而且随着文件中代码行的增多  要import 总是要上下滚动条  非常麻烦
 
 另外 由于hyrobot执行顺序是先从  case文件执行的 所以我们初始化函数一定要放在  框架首先执行的最前面进行初始化 否则找不到 初始化数据 会报错
 
 python  日期序列  自动生成器  是算法  所以序列是根据算法边生成边循环
