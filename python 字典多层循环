各自的数据 放在各自的 方法中 这些数据都是 局部的随便改 
出问题查看数据 也容易

正常场景的数据 放在局部变量一套
参数验证放在局部变量中一套
用来copy.deepcopy 然后每次改一个值
动态发送请求
把参数校验的数据放在字典中
特殊符号弄一组然后随机取
判断是特殊符号就随机取 既然是特殊符号 用 '$' in 特殊符号变量中肯定能判断出是特殊符号
我们要根据key来进行for循环重新赋值  赋值是从 测试数据中取的 也要for循环  要两层for循环所以我们有的时候 只用key来循环就行了
